/*
v.resize()
tum kar sakte ho for making the size of appropriate size
 for vectors
 vector<int>v;
 v.resize(n+1,0);

 for matrix;
 matrix.resize(n+1,vector<int>(n,0));
 vector<vector<int>> v;
 v.resize(m, vector<int>(n, 0));
*/